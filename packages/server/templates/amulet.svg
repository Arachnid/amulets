<svg viewBox="0 0 640 640" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink">
    <style>
        .poem {
            font: 70px Helvetica;
            dominant-baseline: middle;
        }
        .onelinepoem {
            font: 70px Helvetica;
            text-anchor: middle;
            dominant-baseline: middle;
        }
        .lighthex {
            fill: {{lightcolor}};
        }
        .darkhex {
            fill: {{darkcolor}};
        }
    </style>
    <defs>
        <g id="icon">
            <polygon points="40,5 75,25 75,55 40,75 5,55 5,25" class="darkhex" />
            <polygon points="40,5 40,15 15,31 15,47, 5,55, 5,25" class="lighthex" />
        </g>
    </defs>
    {% for char in hash -%}
        {%- if char == 56 -%}
            <use x="{{(loop.index % 8) * 80}}" y="{{(loop.index // 8) * 80}}" xlink:href="#icon" />
        {% endif %}
    {%- endfor %}
    {% if wrapped|length == 1 %}
        <text class="onelinepoem" x="320" y="320">{{wrapped[0]}}</text>
    {% else %}
        {% for line in wrapped %}
            <text class="poem" x="0" y="{{(loop.index - 1) * 80 + 40}}">{{line}}</text>
        {% endfor %}
    {% endif %}
</svg>
